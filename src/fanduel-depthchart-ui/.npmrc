# Vite 8's peer-deps want esbuild ^0.27 || ^0.28, but rolldown's transitive
# graph pins esbuild@0.25 for some bindings. Until Vite 8 / rolldown
# stabilise their peer-dep tree we let npm resolve loosely.
legacy-peer-deps=true
